import { Content } from "../content";

export const SaveTheDateTab = () => {

    return (
        <div>
            <Content children={
                <div>{'Hello'}</div>
            }/>
            <Content children={
                <div>{'August 29, 2026'}</div>
            }/>
        </div>
    );
}